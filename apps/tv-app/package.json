{
  "name": "@momento/tv-app",
  "private": true,
  "version": "0.1.0",
  "description": "Momento living media wall screensaver — TV rendering engine (Tizen/webOS/browser)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
